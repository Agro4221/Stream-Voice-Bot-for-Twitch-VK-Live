# Stream Voice Bot — журнал действий и handoff

Этот файл хранит фактически выполненные действия и текущее состояние проекта, чтобы работу можно было продолжить в новом чате без потери контекста.

## 2026-09-22

### Подтверждено пользователем
- Twitch TTS работает.
- VK Видео Live TTS работает.
- Пользователь хочет сохранить ручной запуск STT кнопкой «Запустить STT».
- После нескольких экспериментальных изменений субтитры стали работать хуже; пользователь сообщил о задержке 4–5 секунд и плохом распознавании/переводе.

### Субтитры / STT
- Отменён экспериментальный SSE Browser Source.
- Отменён агрессивный алгоритм выбрасывания старого аудио.
- Восстановлен проверенный путь Browser Source через /api/state.
- Восстановлена проверенная обработка STT с chunk 2.5 сек и overlap 0.25 сек.
- Убран экспериментальный `without_timestamps=True`.
- Автозапуск STT при открытии /subtitles добавлялся временно, затем удалён по просьбе пользователя.
- Ручной запуск STT сохранён.
- Сохранены прежние Windows-исправления PortAudio/WASAPI: native/default rate, fallback устройства, resampling и COM initialization.

### UI
- Исправлен индикатор «Есть несохранённые изменения» у Silero после сохранения.
- Twitch Client ID скрыт в поле интерфейса.
- Кнопка завершения переименована в «Выключить».
- Статус завершения: «Выключение…» → «Выключен».
- После выключения показывается окно «Бот выключен».
- Twitch TTS упрощён: используется только награда «Озвучить сообщение».
- Обычный Twitch chat не является источником TTS.
- VK ordinary chat фильтруется; TTS принимается только из системного объявления ChatBot о награде.

### Windows / упаковка
- Один пользовательский `StreamVoiceBot.exe`.
- EXE собирается PyInstaller onedir.
- Silero V5 входит в пакет.
- Node нужен только для VK bridge.
- В build pipeline добавлен optional Authenticode hook. Без сертификата EXE остаётся неподписанным.
- Иконка берётся из `Agro4221/-Content-Factory` (`favicon.png`) и преобразуется в ICO на этапе Windows-сборки.
- Добавлен `Create_Desktop_Shortcut.cmd`, который создаёт `Stream Voice Bot.lnk` на рабочем столе и использует иконку EXE.

### Последняя подтверждённая сборка перед текущей чисткой
- Build Windows EXE candidate #51.
- Commit: `fef9aa4904601bb2cbc92f4fa9c37429b2072a72`.
- Artifact ID: `10688238374`.
- CI и EXE smoke-test этой сборки прошли успешно.

### Текущая чистка release-пакета
Сделано в ветке `exe-packaging-v1.1.9-release-candidate`:
- Из PyInstaller datas исключён исходный PNG иконки: он не нужен приложению во время работы.
- Вместо упаковки всей папки `.runtime/node` в EXE-пакет включается только `.runtime/node/node.exe`, потому что npm и прочие инструменты Node нужны только во время сборки, а не при запуске VK bridge.
- Для EXE вместо отдельного скрипта ярлык теперь создаётся автоматически при первом запуске `StreamVoiceBot.exe`.
- Packaging contract тест обновлён под эти условия.
- Добавлен этот файл журнала.

## Коммиты этой сессии
- `66e4e03d1d3557f26f602f98d8f38232303046e7` — возврат ручного запуска STT и проверка сборки.
- `1c369fe622ea043bb2e8a8a7491e83b2a654669d` — упаковка только runtime-файла Node `node.exe`, исключение исходного PNG из runtime datas.
- `dcd7d627700582e78257e3afdecb9fa1fda23e42` — включение скрипта создания ярлыка в release-папку.
- `f9216cbb51d7764e78fc71dd1246f71aa98225a9` — контрактный тест очищенного EXE-пакета.
- `4e064fb6951df07c6ca82b60e982771719431394` — этот журнал действий.

## Не менять без отдельной проверки
- Не менять рабочую логику Twitch/VK TTS.
- Не переводить STT на автостарт.
- Не возвращать SSE или агрессивный drop аудио без ручной проверки.
- Не удалять локальные большие каталоги пользователя (`dist\StreamVoiceBot_1.1.9`, `.venv` и т.п.) без явного разрешения.
- Не публиковать финальный GitHub Release до подтверждения субтитров на Windows.

## Следующий порядок
1. Проверка пользователем последней сборки субтитров.
2. Если субтитры подтверждены — очередная чистая EXE-сборка с упаковочными изменениями.
3. Проверка состава release-папки и отсутствия очевидного мусора.
4. Подготовка финального GitHub Release.
5. При наличии сертификата — включить Authenticode signing через GitHub Secrets.

## 2026-09-22 — восстановление стабильной схемы STT
- Проверены исторические версии v1.0.9/v1.1.0: базовые STT-значения — chunk 4.0 сек, overlap 0.5 сек, beam 1, VAD включён.
- В release-candidate ветке восстановлены chunk 4.0 / overlap 0.5 и последовательный перевод после распознавания.
- Сохранены текущие Windows-исправления audio input, resampling, WASAPI/COM и CPU int8 для frozen EXE.
- Добавлена однократная миграция существующих установок с экспериментальных значений 2.0/2.5 + 0.25 на 4.0/0.5.
- UI чистой установки показывает 4.0 / 0.5.
- Версия кандидата — 1.1.11, так как v1.1.9 и v1.1.10 уже существуют в GitHub Releases.
- Следующий шаг: Windows EXE candidate build → пользовательский тест TTS + ручной STT + OBS subtitles → после подтверждения финальный релиз.


## 2026-09-22 — финальная зачистка кандидата перед ручным одобрением
- Проверена текущая реализация STT: стабильные значения 4.0 сек / 0.5 сек, beam 1, VAD, последовательный локальный перевод, ручной запуск сохранён.
- В STT удалён мёртвый фрагмент с недостижимым вторым `raise` и неиспользуемая переменная; bounded audio deque `maxlen=200` сохранён как ограничитель памяти.
- Для frozen EXE ярлык рабочего стола перенесён из отдельного `Create_Desktop_Shortcut.cmd` непосредственно в первый запуск `StreamVoiceBot.exe`.
- Состояние создания ярлыка фиксируется в `data/.desktop_shortcut_created`; повторные запуски той же копии приложения не пересоздают ярлык.
- Отдельный cmd-скрипт создания ярлыка удалён из репозитория и из release-пакета.
- Сборщик теперь очищает старые `StreamVoiceBot*` каталоги именно в `dist_release` перед новой сборкой.
- Candidate и final release workflows дополнены проверкой автоматического ярлыка и очисткой runtime-файлов перед публикацией артефакта/архива.
- Финальный GitHub Release по-прежнему НЕ публикуется до ручного подтверждения Windows-теста пользователем.


### 2026-09-22 — исправление задержки субтитров после ручного теста
- По фактическому логу пользователя подтверждено, что текущая на тот момент схема 4,0/0,5 давала заметные интервалы между распознанными фразами.
- Проверена история ветки: схема STT менялась несколько раз — 1,0/0,1 → 1,5/0,15 → 2,5/0,25 → 4,0/0,5; поэтому предположение, что схема всегда была одинаковой, не подтвердилось.
- Восстановлены 2,5/0,25 как основной live-caption профиль, но с важным низколатентным правилом: при отставании Whisper используется самое новое окно, а накопленный старый backlog отбрасывается.
- Локальный перевод снова вынесен в отдельные фоновые потоки, чтобы Argos не задерживал следующий цикл STT.
- Сохранены `condition_on_previous_text=False`, VAD и удаление экспериментального `without_timestamps=True`.
- Для существующих установок с 4,0/0,5 добавлена однократная миграция на 2,5/0,25.
- TTS не изменялся; подтверждённый рабочий Twitch/VK TTS не затрагивался.
